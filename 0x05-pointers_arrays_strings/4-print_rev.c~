#include "main.h"

/**
 *print_rev - prints a string in reverse
 *@s: string to reverse and print
 *Return: void
 */

void print_rev(char *s)
{
int i, len = strlen(s);

for (i = len - 1; i >= 0; i--)
{
_putchar(s[i]);
}
_putchar('\n');
}

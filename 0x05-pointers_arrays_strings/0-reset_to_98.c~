#include "main.h"

/**
 * main - takes a pointer to an int as parameter and
 *updates the value it points to to 98
 *@*n: the pointer to check
 * Return: Always 0.
 */
void reset_to_98(int *n)
{
if (n != NULL)
{
*n = 98;
}
}
